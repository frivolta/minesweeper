import React from "react";
import { render } from "react-dom";
import { GameWithHooks } from "@/modules/GameWithHooks/GameWithHooks";

const rootElement = document.getElementById("root");

render(<GameWithHooks />, rootElement);
